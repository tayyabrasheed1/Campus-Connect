{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm install && cd \"App Ui\" && npm install && cd ../backend && npm install",
  "buildCommand": "cd \"App Ui\" && npm run build",
  "outputDirectory": "App Ui/dist",
  "functions": {
    "api/[...path].js": {
      "maxDuration": 30
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/[...path].js"
    },
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "no-store"
        }
      ]
    }
  ]
}
